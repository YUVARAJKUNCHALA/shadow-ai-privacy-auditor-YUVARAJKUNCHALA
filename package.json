{
  "name": "shadow-ai-privacy-auditor",
  "version": "1.0.0",
  "description": "Local-first Shadow AI Privacy Auditor for detecting and redacting sensitive text before it is pasted into public AI tools.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev-server.js",
    "start": "node scripts/dev-server.js",
    "test": "node --test tests/*.test.js",
    "evaluate": "node scripts/evaluate.js"
  },
  "engines": {
    "node": ">=20"
  }
}
